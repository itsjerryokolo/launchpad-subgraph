{
  "name": "memecoinmonitor-launchpad",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ memecoinmonitor-launchpad",
    "deploy:studio": "graph deploy --studio memecoinmonitor-launchpad",
    "create-local": "graph create --node http://localhost:8020/ memecoinmonitor-launchpad",
    "remove-local": "graph remove --node http://localhost:8020/ memecoinmonitor-launchpad",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 memecoinmonitor-launchpad",
    "test": "graph test",
    "benchmark": "bun benchmark.js"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.70.0",
    "@graphprotocol/graph-ts": "0.32.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
